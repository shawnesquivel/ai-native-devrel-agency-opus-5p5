# Supabase project URL and secret (service role) key. Server only.
SUPABASE_URL=
SUPABASE_SECRET_KEY=

# Optional: Slack incoming webhook that receives a message for every new request.
SLACK_WEBHOOK_URL=

# Optional: new requests each client can file per rolling 24 hours (default 10).
DEVREL_DAILY_REQUEST_LIMIT=
